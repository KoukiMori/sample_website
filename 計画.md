# 計画

## さくらのレンタルサーバーへの上げ方（FTP）

このプロジェクトはほぼ静的HTML／CSS／JSに加え、管理画面用のPHP（`admin/save.php`）です。さくらのレンタルサーバーへはFTPでドキュメントルートにアップロードします。

### 1. さくら側の準備

1. [会員メニュー](https://secure.sakura.ad.jp/menu/) → 対象サーバ → **サーバコントロールパネル**
2. **FTP**（または「ファイルマネージャー」）で接続情報を確認
   - FTPホスト（例: `xxxxx.sakura.ne.jp`）
   - ユーザ名／パスワード
3. 公開フォルダは通常 **`www`**（契約によっては `www/ドメイン名`）

管理画面を使う場合は、PHPが動作するプランである必要があります（標準プランで可）。

### 2. FTPソフト

次のいずれかを使う。

- [FileZilla](https://filezilla-project.org/)
- [Cyberduck](https://cyberduck.io/)

接続例（FileZilla）:

| 項目 | 値 |
|------|-----|
| ホスト | `shima-fukushi.sakura.ne.jp`（先頭に ftp. は付けない） |
| ユーザ | `shima-fukushi` |
| パスワード | コントロールパネルのFTPパスワード |
| ポート | `21` |

**クイック接続ではなく「サイトを管理」から接続する。**

1. ファイル → サイトを管理 → 新しいサイト
2. プロトコル: FTP
3. 暗号化: まず「明示的な FTP over TLS が必要」。失敗したら「平文の FTP を使用」
4. 転送設定タブ: **パッシブ**（アクティブだとタイムアウトしやすい）
5. 文字セット: 強制 UTF-8
6. 設定の「タイムアウト」を `60` 秒にする（初期の20秒だとさくらで切れやすい）

左側＝自分のMac、右側＝サーバ。公開フォルダは通常 **`www`**。

### 繋がらないとき

画面の「非アクティブになってから 20 秒後にタイムアウト」「サーバーに接続できません」は、パスワード以前に **21番ポートまで届いていない** 状態です。

1. サイトマネージャーでパッシブ＋タイムアウト60秒にする（クイック接続は使わない）
2. さくらの **サーバコントロールパネル → FTP** で、接続元制限がオンなら今の回線のIPを許可する
3. 会社・学校・モバイル回線は21番を塞いでいることがある。自宅Wi-Fiやスマホのテザリングで試す
4. FTPが使えないときは、コントロールパネルの **ファイルマネージャー** から `www` へアップロードする（ブラウザだけで可）

### 3. 上げるもの／上げないもの

**上げる（ドキュメントルート直下へ）**

- `index.html`、`style.css` などルートの公開ファイル
- `admin/` `assets/` `data/` `facilities/` `other/` `js/` などサイト用フォルダ一式

**上げない**

- `.git/`
- `.cursor/` `.vscode/`
- `.DS_Store`
- `node_modules/`（あれば）
- ローカル専用のメモ類

`www` の中身は、ローカルのプロジェクト直下と同じ並びにする。

```
www/
  index.html
  admin/
  assets/
  data/
  facilities/
  other/
  js/
  ...
```

### 4. アップロード後にやること

1. ブラウザで `https://あなたのドメイン/` を開き、トップが表示されるか確認
2. `https://あなたのドメイン/admin/` で管理画面が開けるか確認
3. **書き込み権限**（管理画面でJSON・PDF・画像を保存するため）
   - `data/`
   - `assets/`（と中の施設フォルダ）
   をFTPソフトで権限 `755`、必要なら `707` / `777` に調整（さくらの案内に合わせる）

権限が足りないと「サーバーに保存」で失敗する。

### 5. 注意点

- 初回はファイルが多いので、フォルダごとドラッグでまとめて上げると楽
- 途中で切れたら、同じ場所に再アップロード（上書き）してよい
- 管理パスワードは `admin/save.php` の初期値、または変更後の `admin/password.php`。本番では必ず変更する
- 日本語ファイル名のPDF・写真もあるため、転送モードは **バイナリ**（FileZillaの「自動」で通常OK）
- 日本語のファイル名は、FTPの文字コードが違うとサーバー上の名前とページのパスがずれて画像が出ない。FileZillaではサイトマネージャー → 文字セット → **強制UTF-8** にする
- HTMLの転送は **バイナリ**。テキスト転送だと「年間行事」「概要」などの一部の文字だけ文字化けする。FileZillaは転送モードをバイナリにする
- 写真は可能なら管理画面から保存する。FTPで直接置く場合は、JSONに書いてあるファイル名（拡張子の大文字小文字も）とサーバー上の名前を同じにする

### 6. チェックリスト

- [ ] FTPで `www` に接続できた
- [ ] `index.html` が `www` 直下にある
- [ ] トップページが表示される
- [ ] `/admin/` が開く
- [ ] お知らせや重要事項などの保存ができる（`data/`・`assets/` の権限）
